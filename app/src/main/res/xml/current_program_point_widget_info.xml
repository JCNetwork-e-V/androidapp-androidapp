<?xml version="1.0" encoding="utf-8"?>
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:initialKeyguardLayout="@layout/current_program_point_widget"
    android:initialLayout="@layout/current_program_point_widget"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:previewImage="@drawable/example"
    android:updatePeriodMillis="1800000"
    android:widgetCategory="home_screen">
    <!-- The update Interval is originally set to 86400000 milliseconds (24 hours)
    We change it here to the most frequent which is 30 min (android:updatePeriodMillis="1800000") -->
</appwidget-provider>