<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    xmlns:app="http://schemas.android.com/apk/res-auto">

<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/instruction_tv"
        android:text="@string/days_register_instruction_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <!-- Normal days -->
    <ImageButton
        android:id="@+id/normal_days_image"
        android:src="@drawable/normal_days_logo"
        android:layout_width="0dp"
        android:layout_height="150dp"
        android:scaleType="centerInside"
        android:background="@color/white"
        android:elevation="8dp"
        android:layout_margin="8dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/instruction_tv"
        android:contentDescription="@string/jcnetwork_days_logo" />

    <!-- Executive days -->
    <ImageButton
        android:id="@+id/executive_days_image"
        android:src="@drawable/executive_days_logo"
        android:layout_width="0dp"
        android:layout_height="150dp"
        android:scaleType="centerInside"
        android:background="@color/white"
        android:elevation="8dp"
        android:layout_margin="8dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/normal_days_image"
        android:contentDescription="@string/jcnetwork_executive_days_logo" />

    <!-- Development days -->
    <ImageButton
        android:id="@+id/development_days_image"
        android:src="@drawable/development_days_logo"
        android:layout_width="0dp"
        android:layout_height="150dp"
        android:scaleType="centerInside"
        android:background="@color/white"
        android:elevation="8dp"
        android:layout_margin="8dp"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/executive_days_image"
        android:contentDescription="@string/jcnetwork_development_days_logo" />

    <Space
        android:layout_width="match_parent"
        android:layout_height="16dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/development_days_image"/>

</androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>